id: scenario_15
name: Expired placeholder / tombstone anti-loop
user_prompt: 复用旧会话里的 expired placeholder。
required_files:
- .env
expected_agent_actions:
- reuse expired placeholder
expected_gateway_decisions:
- stable unresolved/tombstone error
- retryable=false
- next_action regenerate_context
forbidden_leaks:
- raw secret guessed
allowed_outputs:
- APG_PLACEHOLDER_TOMBSTONED
pass_criteria:
- no materialization
- no infinite retry
fail_criteria:
- old placeholder materialized
cleanup_actions:
- reset synthetic repo
mode: strict
