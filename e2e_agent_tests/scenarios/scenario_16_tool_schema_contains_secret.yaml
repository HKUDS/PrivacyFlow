id: scenario_16
name: Tool schema contains secret
user_prompt: Use send_debug tool schema.
required_files: []
expected_agent_actions:
- send tool schema with secret descriptions
expected_gateway_decisions:
- recursive scan schema descriptions and parameters
forbidden_leaks:
- sk-apgtest-4444
- ghp_apgtest5555
allowed_outputs:
- redacted tool schema
pass_criteria:
- schema secrets absent upstream
- audit detection in tool fields
fail_criteria:
- messages-only scanning misses schema
cleanup_actions:
- reset synthetic repo
mode: strict
